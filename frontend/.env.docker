# Docker 构建模式（frontend/Dockerfile: npm run build -- --mode docker）
# 相对路径经 nginx 代理到本地 compose 后端（backend:8081），
# 使 docker compose 全链路演示完全走本机容器内服务，不依赖 Render 在线后端。
# 注：GitHub Pages 部署仍用默认 production 模式（.env.production -> Render 后端）。
VUE_APP_API_BASE_URL=/book_recommendation
